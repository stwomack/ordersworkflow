package com.womack.ordersworkflow.services;

//TODO Pull the worker into its own bean so it can eventually be moved to its own project and scaled independently of the workflow execution generation
public class WorkerService {
}
